16.Write a python code to perform the following database operations using SQLite(CRUD Operations)
17.Write the python code to perform the following operations to clean the data
 a). Missing values
 b).SUm of missing values
 c).REplace with MEan of the column
18. Implement the data cleaning operations on any dta set URL
19.Write a python code to detect the Outliers
20.Perform the following operations on data distributions
   a). Normal distributions
   b).left skewness
   c).Right skewness
